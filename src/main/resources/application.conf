ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        watch = [ classes, resources ]
    }
    application {
        modules = [ com.dinarastepina.ApplicationKt.module ]
    }
    mongoUri = ${MONGO_URI}
}
